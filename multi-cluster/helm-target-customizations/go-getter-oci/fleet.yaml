# This file and all contents in it are OPTIONAL.

# The namespace this chart will be installed and restricted to,
# if not specified the chart will be installed to "default"
namespace: fleet-mc-helm-target-customizations-example-go-getter-oci

# Custom helm options
helm:
  # The release name to use. If empty a generated release name will be used
  releaseName: kubernetes-dashboard

  # The directory of the chart in the repo.  Also any valid go-getter supported
  # URL can be used there is specify where to download the chart from.
  # If repo below is set this value if the chart name in the repo
  chart: "https://kubernetes.github.io/dashboard/kubernetes-dashboard-5.10.0.tgz"

targetCustomizations:
- name: test
  helm:
    chart: "oci://northamerica-northeast1-docker.pkg.dev/romejoe-fleet-dev/kubernetes-dashboard/kubernetes-dashboard"
    version: "5.11.0"
  clusterSelector:
    matchLabels:
      env: test
